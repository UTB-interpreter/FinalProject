#pragma once		// i believe that it is better than ifndef method.

void message();		// to link our share the message() function that comes from message.cpp

struct test{};		// it gives us error if we use this header file twice.